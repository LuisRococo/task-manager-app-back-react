<% presenter = PlanPresenter.new(@plan) %>

<div class="container mb-5">
    <%= render 'pg_header', plan_new_header %>

    <%= render 'model_errors', obj: @plan %>

    <div class="row d-flex justify-content-center">
        <div class="col-12 col-lg-7">
            <%= render 'form' %>
        </div>
    </div>
</div>
